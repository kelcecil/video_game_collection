<h1><%= @user.name %>'s Collection</h1>

<%= for game <- @collection do %>
    <div class="index-row">
        <h3><%= game.name %></h3>
        <p><%= game.publisher %></p>

        <a href="<%= Routes.collection_path(@conn, :edit, game.id) %>">Edit</a>
    </div>
<% end %>

<p>Games in collection: <%= length(@collection) %></p>
